(()=>{const escape=value=>String(value??'').replace(/[&<>'"]/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[char]));UI.dataTable=({headers=[],rows=[]})=>`<div class="data-table-wrap"><table class="data-table"><thead><tr>${headers.map(header=>`<th>${escape(header)}</th>`).join('')}</tr></thead><tbody>${rows.length?rows.map(row=>`<tr>${row.map(cell=>`<td>${escape(cell)}</td>`).join('')}</tr>`).join(''):`<tr><td colspan="${headers.length}">No rows available.</td></tr>`}</tbody></table></div>`;const polishDynamicContent=()=>{const control=document.querySelector('#strategy-channels'),channels=window.LumenStrategy?.getState?.().channels;if(control&&channels)[...control.options].forEach(option=>option.selected=channels.includes(option.value));document.querySelectorAll('select').forEach(select=>{if(select.options.length===1&&select.id==='city-criteria')select.closest('.control')?.remove()})};new MutationObserver(polishDynamicContent).observe(document.querySelector('#content'),{childList:true,subtree:true});if(!location.hash)location.hash='/';polishDynamicContent()})();
