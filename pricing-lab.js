(()=>{
  const content=document.querySelector('#content'),routeName=document.querySelector('#route-name');
  const euro=value=>new Intl.NumberFormat('de-DE',{style:'currency',currency:'EUR',minimumFractionDigits:2,maximumFractionDigits:2}).format(value);
  const renderPage=(data,price)=>{const selected=data.options.find(item=>item.price===price)||data.options[0],rows=data.options.map(item=>[euro(item.price),`${item.acceptance.toFixed(1)}%`,euro(item.netRevenue),euro(item.contribution),`${item.margin.toFixed(1)}%`]);content.innerHTML=UI.pageHeader({eyebrow:'Optimise / pricing lab',title:'Pricing Lab',description:'Compare supplied price-test outcomes. Changing the selection changes the displayed source-backed scenario; it does not create a recommendation.'})+UI.section({title:'Scenario input',description:'Choose one of the candidate prices available in the supplied price-test data.',body:UI.filterControls({body:UI.select({id:'pricing-choice',label:'Candidate retail price',value:String(selected.price),options:data.options.map(item=>({label:euro(item.price),value:String(item.price)}))})+UI.state({type:'mock',message:'Scenario selection only. Demand volume and revenue totals are unavailable in the supplied price-test interface.'})})})+UI.kpiGroup({body:UI.metricCard({label:'Estimated acceptance',value:`${selected.acceptance.toFixed(1)}%`,context:'Supplied test result'})+UI.metricCard({label:'Average contribution / unit',value:euro(selected.contribution),context:'Average across supplied channels'})+UI.metricCard({label:'Average contribution margin',value:`${selected.margin.toFixed(1)}%`,context:'Average across supplied channels'})})+UI.section({title:'Candidate price comparison',description:'Acceptance and per-unit economics from the supplied price-test results. Volume and total revenue are not available.',body:UI.dataTable({headers:['Price','Acceptance','Avg. net revenue / unit','Avg. contribution / unit','Avg. margin'],rows})})+UI.twoColumn({primary:UI.section({title:'Customer and competitor reference',description:'Price-sensitivity thresholds and competitor single-can range are contextual references, not a pricing decision.',body:UI.dataTable({headers:['Reference','Observed value'],rows:[['Average “too cheap” threshold',euro(data.thresholds.cheap)],['Average “too expensive” threshold',euro(data.thresholds.expensive)],['Competitor single-can price range',data.competitorRange?`${euro(data.competitorRange.min)}–${euro(data.competitorRange.max)}`:'Unavailable'],['Reported COGS / unit',data.cogs?euro(data.cogs):'Unavailable']]})}),secondary:UI.section({title:'Business trade-off',description:'Review the available evidence before choosing a pricing direction.',body:UI.tradeoffCard({title:'Acceptance, unit economics, and positioning',body:'The supplied price test shows how stated acceptance and per-unit contribution vary by candidate price. Premium positioning and expected volume are not directly measured here; use them as separate decision inputs.'})+UI.businessImplication({body:'Use this comparison with channel economics, customer thresholds, and competitor context. The page does not select or recommend a price.'})})});UI.activate(content);content.querySelector('#pricing-choice').addEventListener('change',event=>renderPage(data,Number(event.target.value)))};
  const load=async()=>{if(location.hash.slice(1)!=='/optimise/pricing')return;routeName.textContent='Pricing Lab';content.innerHTML=UI.pageHeader({eyebrow:'Optimise',title:'Pricing Lab',description:'Loading supplied price-test data.'})+UI.state({type:'loading'});try{renderPage(await DiscoverData.pricingLab(),2.19)}catch(error){content.innerHTML=UI.pageHeader({eyebrow:'Optimise',title:'Pricing Lab',description:'Price-test data could not be loaded.'})+UI.state({type:'error',message:'Pricing data is unavailable. Check the supplied CSV files when deployed.'})}};
  window.addEventListener('hashchange',load);load();
})();
